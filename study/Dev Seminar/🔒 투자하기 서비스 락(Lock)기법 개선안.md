# 🔒 투자하기 서비스 락(Lock)기법 개선안

#공부 #Lock #DB #SPRING #동시성

---

# hello 투자 flow

1. *investView.html* / 상품 선택 후 투자하기버튼 클릭
2. *custom.invest.js* / kyc체크, 개인투자자 체크 -> go-invest modal(investView.html)
3. *go-invest modal* / 투자위험 동의체크, 유효성 체크 후 투자 실행 -> InvestController
4. *InvestController* / MP_INVEST investAction -> investService.investAction
5. *investService.investAction* / 투자신청기록, api상태체크 -> SERVICE 프로젝트
6. *SERVICE 프로젝트* investmentsService.p2pCenterInvestmentRegister(투자신청기록)
7. *p2p(금결원) 투자신청기록* / 투자 유효성검사(시간 내 재투자, 금액체크) -> p2p센터 투자잔액 조회(api)
8. 잔액조회 후 / 수익률,기간 업데이트 등등 .. 로직 처리 후 **P2PCenter 투자신청 등록**
9. 여기까지 정상 처리되면, *async(비동기)로 자동기표처리*

---

# scf의 상품 특성과 이해

- 초기에는 동시에 치고들어오는 클라이언트가 굉장히 많았다.
- 원활한 서비스를 제공과 데이터 정합성을 유지하기 위해 대기열 서비스가 아닌 락 기법을 사용.

- *동시성 이슈를 대응해야할 곳.*
	1. 모집금액과 동일 차주 투자 한도를 넘지 않게 상품의 동시성
	2. p2p투자한도와 예치금 한도를 넘지 않게 회원별 동시성
	이 두 가지의 동시성 문제를 해결해야한다.

- 우리는 외부은행에 투자자,대출자 돈을 모두 맡겨두고 운영(신탁 운영)중이다.
	-> 따라서, 외부 api를 굉장히 많이 사용한다.
	*한도를 금결원 api를 통해 매번 체크*

- 실제 돈이 빠져나가는 시점은, 모든 투자기록을 마친 후, 대출을 실행하는 시점에서 돈을 가져간다.
	-> 만원만 있어도 여러 상품에 만원씩 여러번 투자가 가능하다. (문제 해결해야함)
	*회원별 동시성*

- 락을 잡아놓고(lock안에서의 서비스로직) api통신을 하는이유?
	동시성 이슈떄문

- 세마포어 뮤텍스 비관적락 등 여러 방법론이 있지만, 우리는 **레디션**으로 락을 구현하였다.
	-> 그 이유는, k8s를 통해 여러 server를 띄워놓는 분산환경이기 때문.

---
## **언제 대기열 vs 락을 사용할까?**

|사용 사례|대기열(Queue)|락(Lock)|
|---|---|---|
|다수의 프로세스가 동시에 요청을 보낼 때|✅|❌|
|작업을 순차적으로 처리해야 할 때|✅|❌|
|비동기 처리가 가능할 때|✅|❌|
|공유 데이터의 동시 수정이 필요한 경우|❌|✅|
|즉각적인 응답이 필요한 경우|❌|✅|
|분산 환경에서 확장성이 중요한 경우|✅|❌|

- **비동기 작업 처리**(예: 이메일 전송, 주문 처리, 로그 저장) → **대기열(Queue) 사용**
- **공유 데이터 동기화**(예: 재고 감소, 은행 계좌 거래, 트랜잭션 처리) → **락(Lock) 사용**
- **분산 환경에서 높은 확장성이 필요할 경우** → 대기열이 유리
- **데이터 정합성이 가장 중요한 경우** → 락이 유리

---
## *레디션*이란? -> redis를 이용한 락이다.
- 분산환경에서 사용하기 용이함.
- 우리의 서비스 프로젝트는 여러 서버로 이루어져있다.
- 보통의 락기법은 하나의 로컬(서버)에서 구현이 이뤄지기 떄문에분산락기법 -> 레디션 락기법을 사용하였다.
- 왜냐? 우리는 쿠버네티스로 여러 서버(여러 서비스 서버)를 운영하기 때문이다.

---

# 투자하기에서 정확히 어떤 이슈가 있는가?
- 만약의 락이 풀려버리면 데이터 정합성이 무너질 수 있다.
	-> 락에 유효시간이 있기 떄문이다. 4초

- 투자시 순서를 반드시 보장하지 못한다.

>[!tip] 왜 락이 풀리지?
**lock으로 묶여있는 로직 안에 너무 많은 동작이 몰려있다.**
-> 그때, 4초가 넘어 버리게 된다면, 락을 놓아버리고 동시성이 깨질 수 있는 가능성이 있다.

## *싱글스레드 큐 방식의 레디스는 왜 순서를 보장하지 않는가?*
-> 큐 구조이긴 하나, 순차적으로 허가를 준다고 하여도 먼저 작업이 끝난 녀석부터 실행되기 때문에.

---

# 투자 실패시 이전 투자기록까지 실패처리하는 이유??
- 중앙기록관리기관(p2p)에서 해당 상품에 대한 단일 투자기록을 요구.
즉, 하나라도 실패하면 실패한 투자건으로써 보낼수 밖에 없는 규정

---

# 해결 방안

## 1. lock으로 묶여있는 로직 단순화
- 매 투자 건 별로 조회하는 개인별 투자한도를 *redis*로 관리하여, 투자건별 속도 개선
# 2. 먼저 투자를 받은 후, 데이터 순서대로 한도별 투자처리
- 상품을 미리 열어, 투자 금액 데이터를 쌓는 원리.
- 해당 방법을 사용하면 데이터 정합성 및 경합을위한 락 사용 불필요

*주의 ! 투자 완료 후 실패 처리를 받을 가능성 존재*

>[!quote] 결론
> 현재 신탁 운영으로 인한 한도 데이터를 직접관리하지 못하는 문제와,
> 보수적인 운영상의 방침으로 큰 구조 개선을 힘들어 보임
>  
> 현재는 리펙토링을 통해 lock에 묶여있는 로직을 단순화 하는것에 만족해야겠음.

